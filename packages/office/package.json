{
  "name": "office",
  "private": true,
  "scripts": {
    "build": "nuxt build  --dotenv ../../.env.deploy.dev",
    "dev": "nuxt dev .playground --dotenv ../../../.env.dev --port 3000 --https --ssl-cert ../../localhost.pem --ssl-key ../../localhost-key.pem\n",
    "generate": "nuxt generate  --dotenv ../../.env.deploy.dev",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@types/node": "^18",
    "@types/office-js": "^1.0.330",
    "dp-api": "workspace:*",
    "nuxt": "^3.5.2"
  },
  "dependencies": {
    "@microsoft/office-js": "^1.1.86",
    "blob-util": "^2.0.2"
  }
}
