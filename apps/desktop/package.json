{
  "name": "desktop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "app:dev": "nuxt dev --dotenv ../../.env.dev --port 3000",
    "generate": "nuxt generate --dotenv ../../.env.deploy.dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.6",
    "@types/node": "^18.11.18",
    "dp-api": "workspace:*",
    "internal-ip": "^7.0.0",
    "nuxt": "^3.4.3",
    "sass": "^1.58.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.1",
    "tauri-plugin-autostart-api": "github:tauri-apps/tauri-plugin-autostart#v1"
  }
}
