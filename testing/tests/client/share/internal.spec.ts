import { test } from '@playwright/test';
import { client, expect } from '../../../utils/client';

test.describe('Internal Share', () => {

    client('start share', async({browsePage}) => {

    })

});